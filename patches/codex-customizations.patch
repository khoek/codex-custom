diff --git a/codex-rs/tui/src/app/tests/safety_buffering.rs b/codex-rs/tui/src/app/tests/safety_buffering.rs
index 6e0e710f21..0d453f448f 100644
--- a/codex-rs/tui/src/app/tests/safety_buffering.rs
+++ b/codex-rs/tui/src/app/tests/safety_buffering.rs
@@ -571,7 +571,7 @@ goals = true
             thread_id: source_thread_id,
             turn_id: active_turn_id,
             model: FASTER_MODEL.to_string(),
-            turn: active_turn,
+            turn: active_turn.clone(),
             prompt: UserMessage::from(RETRY_PROMPT),
         },
     ))
@@ -610,28 +610,12 @@ goals = true
             app.chat_widget
                 .can_retry_safety_buffered_turn(&first_retry_turn_id)
         );
-        app.chat_widget
-            .handle_key_event(KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE));
-        let second_retry = loop {
-            match app_event_rx.try_recv() {
-                Ok(AppEvent::RetrySafetyBufferedTurn {
-                    thread_id,
-                    turn_id,
-                    model,
-                    turn,
-                    prompt,
-                }) => {
-                    break SafetyBufferedRetry {
-                        thread_id,
-                        turn_id,
-                        model,
-                        turn,
-                        prompt,
-                    };
-                }
-                Ok(_) => continue,
-                Err(err) => panic!("expected second safety-buffering retry event: {err}"),
-            }
+        let second_retry = SafetyBufferedRetry {
+            thread_id: first_retry_thread_id,
+            turn_id: first_retry_turn_id,
+            model: FASTER_MODEL.to_string(),
+            turn: active_turn,
+            prompt: UserMessage::from(RETRY_PROMPT),
         };
         let AppCommand::UserTurn { items, .. } = &second_retry.turn else {
             panic!("second safety-buffering retry should retain the user turn");
diff --git a/codex-rs/tui/src/chatwidget/safety_buffering.rs b/codex-rs/tui/src/chatwidget/safety_buffering.rs
index be65e5ee58..491758ec26 100644
--- a/codex-rs/tui/src/chatwidget/safety_buffering.rs
+++ b/codex-rs/tui/src/chatwidget/safety_buffering.rs
@@ -215,5 +215,8 @@ impl ChatWidget {
             items,
             ..Default::default()
         });
+        // Match "Dismiss and keep waiting": close only the modal while the turn continues.
+        self.bottom_pane
+            .dismiss_view_by_id(SAFETY_BUFFERING_PROMPT_VIEW_ID);
     }
 }
diff --git a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__safety_buffering_retry_prompt.snap b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__safety_buffering_retry_prompt.snap
index 767c9c393b..1ee62c7d85 100644
--- a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__safety_buffering_retry_prompt.snap
+++ b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__safety_buffering_retry_prompt.snap
@@ -2,13 +2,12 @@
 source: tui/src/chatwidget/tests/app_server.rs
 expression: popup
 ---
-  Our systems are thinking a bit more about this request before responding.
-  Hang tight or retry with a faster model for a quicker response, though it
-  may be less capable of handling complex requests.
+• Working (0s • esc to interrupt)
+  └ Our systems are thinking a bit more about this request before responding.
+    Hang tight or retry with a faster model for a quicker response, though it
+    may be less capable of handling complex requests.

-› 1. Retry with a faster model
-  2. Dismiss and keep waiting
-  3. Learn more

-  No action is required. Codex will keep waiting, and this menu will close when
-  the response is ready.
+› Ask Codex to do anything
+
+  gpt-5.6-sol default · /tmp/project
diff --git a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__safety_buffering_side_conversation_without_retry.snap b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__safety_buffering_side_conversation_without_retry.snap
index 56efb00764..446058b44c 100644
--- a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__safety_buffering_side_conversation_without_retry.snap
+++ b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__safety_buffering_side_conversation_without_retry.snap
@@ -2,10 +2,10 @@
 source: tui/src/chatwidget/tests/app_server.rs
 expression: popup
 ---
-  Our systems are thinking a bit more about this request before responding.
+• Working (0s • esc to interrupt)
+  └ Our systems are thinking a bit more about this request before responding.

-› 1. Dismiss and keep waiting
-  2. Learn more

-  No action is required. Codex will keep waiting, and this menu will close when
-  the response is ready.
+› Check recently modified functions for compatibility
+
+  gpt-5.6-sol default · /tmp/project
diff --git a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__safety_buffering_status_without_retry.snap b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__safety_buffering_status_without_retry.snap
index 56efb00764..5fafc3e27c 100644
--- a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__safety_buffering_status_without_retry.snap
+++ b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__safety_buffering_status_without_retry.snap
@@ -2,10 +2,10 @@
 source: tui/src/chatwidget/tests/app_server.rs
 expression: popup
 ---
-  Our systems are thinking a bit more about this request before responding.
+• Working (0s • esc to interrupt)
+  └ Our systems are thinking a bit more about this request before responding.

-› 1. Dismiss and keep waiting
-  2. Learn more

-  No action is required. Codex will keep waiting, and this menu will close when
-  the response is ready.
+› Ask Codex to do anything
+
+  gpt-5.6-sol default · /tmp/project
diff --git a/codex-rs/tui/src/chatwidget/tests/app_server.rs b/codex-rs/tui/src/chatwidget/tests/app_server.rs
index 3edeacf40d..6933ecc46f 100644
--- a/codex-rs/tui/src/chatwidget/tests/app_server.rs
+++ b/codex-rs/tui/src/chatwidget/tests/app_server.rs
@@ -111,8 +111,8 @@ fn safety_buffering_notification(
 }

 #[tokio::test]
-async fn safety_buffering_offers_one_retry_with_app_wording() {
-    let (mut chat, mut rx, mut op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
+async fn safety_buffering_retry_prompt_is_dismissed_automatically() {
+    let (mut chat, _rx, mut op_rx) = make_chatwidget_manual(/*model_override*/ None).await;
     let (thread_id, turn_id, _) = start_safety_buffering_test_turn(&mut chat, &mut op_rx);

     let notification = safety_buffering_notification(thread_id, turn_id, Some("faster-model"));
@@ -127,45 +127,10 @@ async fn safety_buffering_offers_one_retry_with_app_wording() {

     let popup = render_bottom_popup(&chat, /*width*/ 80);
     assert_chatwidget_snapshot!("safety_buffering_retry_prompt", popup);
-
-    chat.handle_key_event(KeyEvent::new(KeyCode::Down, KeyModifiers::NONE));
-    chat.handle_key_event(KeyEvent::new(KeyCode::Down, KeyModifiers::NONE));
-    chat.handle_key_event(KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE));
-    let opened_url = loop {
-        match rx.try_recv() {
-            Ok(AppEvent::OpenUrlInBrowser { url }) => break url,
-            Ok(_) => continue,
-            Err(err) => panic!("expected learn-more URL event: {err}"),
-        }
-    };
-    assert_eq!(opened_url, "https://help.openai.com/en/articles/20001326");
-    assert!(render_bottom_popup(&chat, /*width*/ 80).contains(SAFETY_BUFFERING_HEADER_TEXT));
-
-    chat.handle_key_event(KeyEvent::new(KeyCode::Up, KeyModifiers::NONE));
-    chat.handle_key_event(KeyEvent::new(KeyCode::Up, KeyModifiers::NONE));
-    chat.handle_key_event(KeyEvent::new(KeyCode::Enter, KeyModifiers::NONE));
-    let (event_thread_id, event_turn_id, model, turn, prompt) = loop {
-        match rx.try_recv() {
-            Ok(AppEvent::RetrySafetyBufferedTurn {
-                thread_id,
-                turn_id,
-                model,
-                turn,
-                prompt,
-            }) => break (thread_id, turn_id, model, turn, prompt),
-            Ok(_) => continue,
-            Err(err) => panic!("expected safety-buffering retry event: {err}"),
-        }
-    };
-    assert_eq!(event_thread_id, thread_id);
-    assert_eq!(event_turn_id, turn_id);
-    assert_eq!(model, "faster-model");
-    assert_matches!(turn, Op::UserTurn { .. });
-    assert_eq!(prompt, UserMessage::from("Explain the request"));
-    assert!(
-        !render_bottom_popup(&chat, /*width*/ 80)
-            .contains("Press enter to confirm or esc to go back")
-    );
+    assert!(!popup.contains("Retry with a faster model"));
+    assert!(!popup.contains("Dismiss and keep waiting"));
+    assert!(!popup.contains("Learn more"));
+    assert!(chat.can_retry_safety_buffered_turn(turn_id));
 }

 #[tokio::test]
diff --git a/codex-rs/cli/src/main.rs b/codex-rs/cli/src/main.rs
index 034c27fdda..1351799f97 100644
--- a/codex-rs/cli/src/main.rs
+++ b/codex-rs/cli/src/main.rs
@@ -93,7 +93,7 @@ use codex_terminal_detection::TerminalName;
 #[derive(Debug, Parser)]
 #[clap(
     author,
-    version,
+    version = concat!(env!("CARGO_PKG_VERSION"), "+k"),
     // If a sub‑command is given, ignore requirements of the default args.
     subcommand_negates_reqs = true,
     // The executable is sometimes invoked via a platform‑specific name like
diff --git a/codex-rs/tui/src/version.rs b/codex-rs/tui/src/version.rs
index 8c8d108dc6..ada5b67880 100644
--- a/codex-rs/tui/src/version.rs
+++ b/codex-rs/tui/src/version.rs
@@ -1,2 +1,2 @@
 /// The current Codex CLI version as embedded at compile time.
-pub const CODEX_CLI_VERSION: &str = env!("CARGO_PKG_VERSION");
+pub const CODEX_CLI_VERSION: &str = concat!(env!("CARGO_PKG_VERSION"), "+k");
diff --git a/codex-rs/tui/src/app/tests/rate_limits.rs b/codex-rs/tui/src/app/tests/rate_limits.rs
index 1844c7fea6..6da45276f9 100644
--- a/codex-rs/tui/src/app/tests/rate_limits.rs
+++ b/codex-rs/tui/src/app/tests/rate_limits.rs
@@ -277,7 +277,7 @@ async fn stale_rate_limit_reads_preserve_newer_workspace_hard_stop_for_every_ori
 }

 #[tokio::test]
-async fn stale_rate_limit_read_does_not_dismiss_visible_workspace_advisory() -> Result<()> {
+async fn stale_rate_limit_read_does_not_restore_auto_dismissed_model_nudge() -> Result<()> {
     let (mut app, _app_event_rx, _op_rx) = make_test_app_with_channels().await;
     set_chatgpt_auth(&mut app.chat_widget);
     let mut tui = crate::tui::test_support::make_test_tui()?;
@@ -305,7 +305,7 @@ async fn stale_rate_limit_read_does_not_dismiss_visible_workspace_advisory() ->
         /*replay_kind*/ None,
     );
     assert!(
-        render_bottom_popup(&app.chat_widget, /*width*/ 100).contains("Approaching rate limits")
+        !render_bottom_popup(&app.chat_widget, /*width*/ 100).contains("Approaching rate limits")
     );

     Box::pin(app.handle_event(
@@ -324,7 +324,7 @@ async fn stale_rate_limit_read_does_not_dismiss_visible_workspace_advisory() ->
     .await?;

     assert!(
-        render_bottom_popup(&app.chat_widget, /*width*/ 100).contains("Approaching rate limits")
+        !render_bottom_popup(&app.chat_widget, /*width*/ 100).contains("Approaching rate limits")
     );
     app_server.shutdown().await?;
     Ok(())
diff --git a/codex-rs/tui/src/chatwidget/rate_limits.rs b/codex-rs/tui/src/chatwidget/rate_limits.rs
index ba3e10082b..297d45968f 100644
--- a/codex-rs/tui/src/chatwidget/rate_limits.rs
+++ b/codex-rs/tui/src/chatwidget/rate_limits.rs
@@ -442,6 +442,13 @@ impl ChatWidget {
             items,
             ..Default::default()
         });
+        // Match "Keep current model (never show again)": preserve the model and persist the opt-out.
+        self.app_event_tx
+            .send(AppEvent::UpdateRateLimitSwitchPromptHidden(true));
+        self.app_event_tx
+            .send(AppEvent::PersistRateLimitSwitchPromptHidden);
+        self.bottom_pane
+            .dismiss_view_by_id(RATE_LIMIT_SWITCH_PROMPT_VIEW_ID);
     }

     pub(super) fn open_workspace_owner_nudge_prompt(
diff --git a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__rate_limit_switch_prompt_popup.snap b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__rate_limit_switch_prompt_popup.snap
index 9281a3e561..3e9f24f35a 100644
--- a/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__rate_limit_switch_prompt_popup.snap
+++ b/codex-rs/tui/src/chatwidget/snapshots/codex_tui__chatwidget__tests__rate_limit_switch_prompt_popup.snap
@@ -2,13 +2,6 @@
 source: tui/src/chatwidget/tests/status_and_layout.rs
 expression: popup
 ---
-  Approaching rate limits
-  Switch to gpt-5.6-luna for lower credit usage?
+› Ask Codex to do anything

-› 1. Switch to gpt-5.6-luna                 Fast and affordable agentic coding
-                                            model.
-  2. Keep current model
-  3. Keep current model (never show again)  Hide future rate limit reminders
-                                            about switching models.
-
-  Press enter to confirm or esc to go back
+  gpt-5 default · /tmp/project
diff --git a/codex-rs/tui/src/chatwidget/tests/plan_mode.rs b/codex-rs/tui/src/chatwidget/tests/plan_mode.rs
index 079864104e..b014c28a87 100644
--- a/codex-rs/tui/src/chatwidget/tests/plan_mode.rs
+++ b/codex-rs/tui/src/chatwidget/tests/plan_mode.rs
@@ -1118,7 +1118,7 @@ async fn plan_implementation_popup_shows_after_new_plan_follows_steer() {
 }

 #[tokio::test]
-async fn plan_implementation_popup_skips_when_rate_limit_prompt_pending() {
+async fn plan_implementation_popup_stays_skipped_when_rate_limit_prompt_auto_dismisses() {
     let (mut chat, _rx, _op_rx) = make_chatwidget_manual(Some("gpt-5")).await;
     chat.has_chatgpt_account = true;
     chat.set_feature_enabled(Feature::CollaborationModes, /*enabled*/ true);
@@ -1141,8 +1141,8 @@ async fn plan_implementation_popup_skips_when_rate_limit_prompt_pending() {

     let popup = render_bottom_popup(&chat, /*width*/ 80);
     assert!(
-        popup.contains("Approaching rate limits"),
-        "expected rate limit popup, got {popup:?}"
+        !popup.contains("Approaching rate limits"),
+        "expected rate limit popup to be dismissed, got {popup:?}"
     );
     assert!(
         !popup.contains(PLAN_IMPLEMENTATION_TITLE),
diff --git a/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs b/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
index 5719e04dcb..3a7fc65948 100644
--- a/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
+++ b/codex-rs/tui/src/chatwidget/tests/status_and_layout.rs
@@ -1153,7 +1153,7 @@ async fn rate_limit_switch_prompt_clears_pending_when_workspace_credits_become_u
 }

 #[tokio::test]
-async fn rate_limit_switch_prompt_dismisses_shown_when_workspace_credits_become_usable() {
+async fn rate_limit_switch_prompt_stays_dismissed_when_workspace_credits_become_usable() {
     let (mut chat, _, _) = make_chatwidget_manual(Some("gpt-5")).await;
     chat.has_chatgpt_account = true;

@@ -1163,7 +1163,7 @@ async fn rate_limit_switch_prompt_dismisses_shown_when_workspace_credits_become_
         chat.rate_limit_switch_prompt,
         RateLimitSwitchPromptState::Shown
     ));
-    assert!(!chat.bottom_pane.no_modal_or_popup_active());
+    assert!(chat.bottom_pane.no_modal_or_popup_active());

     let mut funded_snapshot = snapshot(/*percent*/ 95.0);
     funded_snapshot.credits = Some(CreditsSnapshot {
@@ -1614,8 +1614,8 @@ async fn rate_limit_switch_prompt_defers_until_task_complete() {
 }

 #[tokio::test]
-async fn rate_limit_switch_prompt_popup_snapshot() {
-    let (mut chat, _rx, _op_rx) = make_chatwidget_manual(Some("gpt-5")).await;
+async fn rate_limit_switch_prompt_is_dismissed_and_disabled_automatically() {
+    let (mut chat, mut rx, _op_rx) = make_chatwidget_manual(Some("gpt-5")).await;
     chat.has_chatgpt_account = true;

     chat.on_rate_limit_snapshot(Some(snapshot(/*percent*/ 92.0)));
@@ -1623,6 +1623,19 @@ async fn rate_limit_switch_prompt_popup_snapshot() {

     let popup = render_bottom_popup(&chat, /*width*/ 80);
     assert_chatwidget_snapshot!("rate_limit_switch_prompt_popup", popup);
+    assert!(!popup.contains("Approaching rate limits"));
+
+    let mut hidden_for_session = false;
+    let mut persisted = false;
+    while let Ok(event) = rx.try_recv() {
+        match event {
+            AppEvent::UpdateRateLimitSwitchPromptHidden(true) => hidden_for_session = true,
+            AppEvent::PersistRateLimitSwitchPromptHidden => persisted = true,
+            _ => {}
+        }
+    }
+    assert!(hidden_for_session);
+    assert!(persisted);
 }

 #[tokio::test]

diff --git a/codex-rs/core/src/responses_retry.rs b/codex-rs/core/src/responses_retry.rs
index dee517c985..5b244eda05 100644
--- a/codex-rs/core/src/responses_retry.rs
+++ b/codex-rs/core/src/responses_retry.rs
@@ -11,6 +11,9 @@ use codex_protocol::protocol::EventMsg;
 use codex_protocol::protocol::WarningEvent;
 use tracing::warn;
 
+const SERVER_OVERLOAD_INITIAL_RETRY_DELAY: Duration = Duration::from_secs(2);
+const SERVER_OVERLOAD_MAX_RETRY_DELAY: Duration = Duration::from_secs(60);
+
 #[derive(Debug, Clone, Copy)]
 pub(crate) enum ResponsesStreamRequest {
     Sampling,
@@ -73,6 +76,35 @@ pub(crate) async fn handle_retryable_response_stream_error(
     Err(err)
 }
 
+pub(crate) async fn handle_server_overload_retry(
+    retries: &mut u64,
+    err: CodexErr,
+    sess: &Session,
+    turn_context: &TurnContext,
+) {
+    *retries = retries.saturating_add(1);
+    let retry_count = *retries;
+    let exponent = retry_count.saturating_sub(1).min(5) as u32;
+    let delay = (SERVER_OVERLOAD_INITIAL_RETRY_DELAY * 2_u32.saturating_pow(exponent))
+        .min(SERVER_OVERLOAD_MAX_RETRY_DELAY);
+    warn!(
+        turn_id = %turn_context.sub_id,
+        retry_count,
+        ?delay,
+        "model at capacity; retrying sampling request"
+    );
+    sess.notify_stream_error(
+        turn_context,
+        format!(
+            "Model at capacity; retrying in {}s (attempt {retry_count})",
+            delay.as_secs()
+        ),
+        err,
+    )
+    .await;
+    tokio::time::sleep(delay).await;
+}
+
 fn log_retry(
     request: ResponsesStreamRequest,
     turn_context: &TurnContext,
diff --git a/codex-rs/core/src/session/mod.rs b/codex-rs/core/src/session/mod.rs
index 0117b596ff..41799e8df3 100644
--- a/codex-rs/core/src/session/mod.rs
+++ b/codex-rs/core/src/session/mod.rs
@@ -3942,14 +3942,19 @@ impl Session {
         message: impl Into<String>,
         codex_error: CodexErr,
     ) {
-        let additional_details = codex_error.to_string();
-        let codex_error_info = CodexErrorInfo::ResponseStreamDisconnected {
-            http_status_code: codex_error.http_status_code_value(),
+        let (codex_error_info, additional_details) = match codex_error.details() {
+            CodexErrorDetails::ServerOverloaded => (codex_error.to_codex_protocol_error(), None),
+            _ => (
+                CodexErrorInfo::ResponseStreamDisconnected {
+                    http_status_code: codex_error.http_status_code_value(),
+                },
+                Some(codex_error.to_string()),
+            ),
         };
         let event = EventMsg::StreamError(StreamErrorEvent {
             message: message.into(),
             codex_error_info: Some(codex_error_info),
-            additional_details: Some(additional_details),
+            additional_details,
         });
         self.send_event(turn_context, event).await;
     }
diff --git a/codex-rs/core/src/session/turn.rs b/codex-rs/core/src/session/turn.rs
index 16ef4e7d36..898532e75e 100644
--- a/codex-rs/core/src/session/turn.rs
+++ b/codex-rs/core/src/session/turn.rs
@@ -37,6 +37,7 @@ use crate::responses_metadata::CodexResponsesMetadata;
 use crate::responses_metadata::CodexResponsesRequestKind;
 use crate::responses_retry::ResponsesStreamRequest;
 use crate::responses_retry::handle_retryable_response_stream_error;
+use crate::responses_retry::handle_server_overload_retry;
 use crate::session::PreviousTurnSettings;
 use crate::session::TurnInput;
 use crate::session::session::Session;
@@ -1332,6 +1333,7 @@ async fn run_sampling_request(
     );
     let max_retries = turn_context.provider.info().stream_max_retries();
     let mut retries = 0;
+    let mut server_overload_retries = 0;
     let mut initial_input = Some(input);
     let mut original_input = None;
     let mut executed_tool_calls_by_output = HashMap::new();
@@ -1392,6 +1394,13 @@ async fn run_sampling_request(
             original_input = Some(prompt.input);
         }
 
+        if matches!(err.details(), CodexErrorDetails::ServerOverloaded) {
+            handle_server_overload_retry(&mut server_overload_retries, err, &sess, &turn_context)
+                .await;
+            turn_context.turn_timing_state.record_sampling_retry();
+            continue;
+        }
+
         if !err.is_retryable() {
             return Err(err);
         }
diff --git a/codex-rs/core/tests/suite/client.rs b/codex-rs/core/tests/suite/client.rs
index 345ca5ff4d..d966ab6002 100644
--- a/codex-rs/core/tests/suite/client.rs
+++ b/codex-rs/core/tests/suite/client.rs
@@ -45,6 +45,7 @@ use codex_protocol::models::ResponseItem;
 use codex_protocol::models::WebSearchAction;
 use codex_protocol::openai_models::InputModality;
 use codex_protocol::openai_models::ReasoningEffort;
+use codex_protocol::protocol::CodexErrorInfo;
 use codex_protocol::protocol::EventMsg;
 use codex_protocol::protocol::Op;
 use codex_protocol::protocol::RolloutItem;
@@ -64,11 +65,13 @@ use core_test_support::responses::ev_function_call;
 use core_test_support::responses::ev_message_item_added;
 use core_test_support::responses::ev_output_text_delta;
 use core_test_support::responses::ev_response_created;
+use core_test_support::responses::mount_response_sequence;
 use core_test_support::responses::mount_sse_once;
 use core_test_support::responses::mount_sse_once_match;
 use core_test_support::responses::mount_sse_sequence;
 use core_test_support::responses::sse;
 use core_test_support::responses::sse_failed;
+use core_test_support::responses::sse_response;
 use core_test_support::responses::strip_metadata_from_json;
 use core_test_support::responses::strip_response_item_ids_from_json;
 use core_test_support::responses_metadata as test_responses_metadata;
@@ -83,6 +86,7 @@ use serde_json::json;
 use std::io::Write;
 use std::num::NonZeroU64;
 use std::sync::Arc;
+use std::time::Duration;
 use tempfile::TempDir;
 use uuid::Uuid;
 use wiremock::Mock;
@@ -3467,6 +3471,114 @@ async fn usage_limit_error_emits_rate_limit_event() -> anyhow::Result<()> {
     Ok(())
 }
 
+#[tokio::test]
+async fn server_overload_uses_transport_budget_then_backs_off_and_resets() -> anyhow::Result<()> {
+    skip_if_no_network!(Ok(()));
+    let server = MockServer::start().await;
+    let overloaded = || {
+        ResponseTemplate::new(503).set_body_json(json!({
+            "error": {
+                "code": "server_is_overloaded",
+                "message": "selected model is at capacity",
+            }
+        }))
+    };
+    let plan_args = json!({
+        "plan": [{"step": "Continue after retry", "status": "completed"}],
+    })
+    .to_string();
+    let responses = mount_response_sequence(
+        &server,
+        vec![
+            overloaded(),
+            overloaded(),
+            sse_response(sse_failed(
+                "overloaded-2",
+                "server_is_overloaded",
+                "selected model is at capacity",
+            )),
+            sse_response(sse(vec![
+                ev_response_created("tool-response"),
+                ev_function_call("plan-call", "update_plan", &plan_args),
+                ev_completed("tool-response"),
+            ])),
+            overloaded(),
+            overloaded(),
+            sse_response(sse(vec![
+                ev_response_created("final-response"),
+                ev_assistant_message("final-message", "done"),
+                ev_completed("final-response"),
+            ])),
+        ],
+    )
+    .await;
+    let test = test_codex()
+        .with_config(|config| {
+            config.model_provider.request_max_retries = Some(1);
+            config.model_provider.stream_max_retries = Some(0);
+        })
+        .build_with_auto_env(&server)
+        .await?;
+    tokio::time::pause();
+
+    test.codex
+        .submit(Op::UserInput {
+            items: vec![UserInput::Text {
+                text: "keep working".into(),
+                text_elements: Vec::new(),
+            }],
+            final_output_json_schema: None,
+            responsesapi_client_metadata: None,
+            additional_context: Default::default(),
+            thread_settings: Default::default(),
+        })
+        .await?;
+
+    let mut retry_events = Vec::new();
+    let mut request_counts_at_retry = Vec::new();
+    loop {
+        match test.codex.next_event().await?.msg {
+            EventMsg::StreamError(event) => {
+                retry_events.push((
+                    event.message,
+                    event.codex_error_info,
+                    event.additional_details,
+                ));
+                request_counts_at_retry.push(responses.requests().len());
+                tokio::time::advance(Duration::from_secs(60)).await;
+            }
+            EventMsg::Error(event) => panic!("capacity retry aborted the turn: {event:?}"),
+            EventMsg::TurnComplete(_) => break,
+            _ => {}
+        }
+    }
+
+    assert_eq!(
+        retry_events,
+        vec![
+            (
+                "Model at capacity; retrying in 2s (attempt 1)".to_string(),
+                Some(CodexErrorInfo::ServerOverloaded),
+                None,
+            ),
+            (
+                "Model at capacity; retrying in 4s (attempt 2)".to_string(),
+                Some(CodexErrorInfo::ServerOverloaded),
+                None,
+            ),
+            (
+                "Model at capacity; retrying in 2s (attempt 1)".to_string(),
+                Some(CodexErrorInfo::ServerOverloaded),
+                None,
+            ),
+        ]
+    );
+    assert_eq!(request_counts_at_retry, vec![2, 3, 6]);
+    assert_eq!(responses.requests().len(), 7);
+
+    Ok(())
+}
+
 #[tokio::test(flavor = "multi_thread", worker_threads = 2)]
 async fn context_window_error_sets_total_tokens_to_model_window() -> anyhow::Result<()> {
     skip_if_no_network!(Ok(()));
