diff --git a/codex-rs/Cargo.toml b/codex-rs/Cargo.toml
index ac80e2ffe8b617e993d46f686ce5adec10ca6add..ff855b687a7de910da9fc29679a2380cdb98b95d 100644
--- a/codex-rs/Cargo.toml
+++ b/codex-rs/Cargo.toml
@@ -154,7 +154,7 @@ members = [
 resolver = "2"
 
 [workspace.package]
-version = "0.0.0"
+version = "0.157.0-k.53446f90"
 # Track the edition for all workspace crates in one place. Individual
 # crates can still override this value, but keeping it here means new
 # crates created with `cargo new -w ...` automatically inherit the 2024
diff --git a/codex-rs/core/src/session/mod.rs b/codex-rs/core/src/session/mod.rs
index b977bb16f6dc9413e63252e2e423c016f4964724..7e3f129344de9cfda81a050527e9b860af8baa29 100644
--- a/codex-rs/core/src/session/mod.rs
+++ b/codex-rs/core/src/session/mod.rs
@@ -4921,14 +4921,19 @@ impl Session {
         message: impl Into<String>,
         codex_error: CodexErr,
     ) {
-        let additional_details = codex_error.to_string();
-        let codex_error_info = CodexErrorInfo::ResponseStreamDisconnected {
-            http_status_code: codex_error.http_status_code_value(),
+        let (codex_error_info, additional_details) = match codex_error.details() {
+            CodexErrorDetails::ServerOverloaded => (codex_error.to_codex_protocol_error(), None),
+            _ => (
+                CodexErrorInfo::ResponseStreamDisconnected {
+                    http_status_code: codex_error.http_status_code_value(),
+                },
+                Some(codex_error.to_string()),
+            ),
         };
         let event = EventMsg::StreamError(StreamErrorEvent {
             message: message.into(),
             codex_error_info: Some(codex_error_info),
-            additional_details: Some(additional_details),
+            additional_details,
         });
         self.send_event(turn_context, event).await;
     }
diff --git a/codex-rs/protocol/src/error.rs b/codex-rs/protocol/src/error.rs
index 9cb401f29fbbde8f18e933301a9b5e11c4964389..1b303dea62702b66644e426d2fd01e2edf4eaf8d 100644
--- a/codex-rs/protocol/src/error.rs
+++ b/codex-rs/protocol/src/error.rs
@@ -405,13 +405,13 @@ impl CodexErr {
             | CodexErrorDetails::Spawn
             | CodexErrorDetails::SessionConfiguredNotFirstEvent
             | CodexErrorDetails::UsageLimitReached(_)
-            | CodexErrorDetails::ServerOverloaded
             | CodexErrorDetails::FlexUnavailable
             | CodexErrorDetails::CyberPolicy { .. }
             | CodexErrorDetails::BioPolicy { .. }
             | CodexErrorDetails::MisalignmentPolicyViolation { .. } => None,
             CodexErrorDetails::Stream(..)
             | CodexErrorDetails::RateLimitExceeded(_)
+            | CodexErrorDetails::ServerOverloaded
             | CodexErrorDetails::Timeout
             | CodexErrorDetails::RequestTimeout
             | CodexErrorDetails::UnexpectedStatus(_)
diff --git a/codex-rs/tui/src/chatwidget/rate_limits.rs b/codex-rs/tui/src/chatwidget/rate_limits.rs
index 36f72ed722f80e4ae0573cc84b7411b6be05611a..383a058ce45cf0fd79a830925e730c7f58236e26 100644
--- a/codex-rs/tui/src/chatwidget/rate_limits.rs
+++ b/codex-rs/tui/src/chatwidget/rate_limits.rs
@@ -535,6 +535,13 @@ impl ChatWidget {
             items,
             ..SelectionViewParams::picker()
         });
+        // Match "Keep current model (never show again)": preserve the model and persist the opt-out.
+        self.app_event_tx
+            .send(AppEvent::UpdateRateLimitSwitchPromptHidden(true));
+        self.app_event_tx
+            .send(AppEvent::PersistRateLimitSwitchPromptHidden);
+        self.bottom_pane
+            .dismiss_view_by_id(RATE_LIMIT_SWITCH_PROMPT_VIEW_ID);
     }
 
     pub(super) fn open_workspace_owner_nudge_prompt(
diff --git a/codex-rs/tui/src/chatwidget/safety_buffering.rs b/codex-rs/tui/src/chatwidget/safety_buffering.rs
index dacfbc5db082d731c0a08dc1ac958c1e0a3aef9d..cec1f79b1e7d1787b28d2c0dbb2ba0b8a8da7e74 100644
--- a/codex-rs/tui/src/chatwidget/safety_buffering.rs
+++ b/codex-rs/tui/src/chatwidget/safety_buffering.rs
@@ -234,6 +234,9 @@ impl ChatWidget {
             items,
             ..SelectionViewParams::picker()
         });
+        // Match "Dismiss and keep waiting": close only the modal while the turn continues.
+        self.bottom_pane
+            .dismiss_view_by_id(SAFETY_BUFFERING_PROMPT_VIEW_ID);
     }
 
     pub(crate) fn confirm_safety_buffered_retry(
diff --git a/codex-rs/tui/src/app/background_requests.rs b/codex-rs/tui/src/app/background_requests.rs
index bfa33203da5025c3de954e7a796cc8dff11d3bb9..d4cc38e3623627a791738337cb6f318008a24b2f 100644
--- a/codex-rs/tui/src/app/background_requests.rs
+++ b/codex-rs/tui/src/app/background_requests.rs
@@ -789,27 +789,45 @@ pub(super) async fn fetch_account_rate_limits(
         .request_typed(ClientRequest::GetAccountRateLimits {
             request_id: request_id.clone(),
             params: Some(GetAccountRateLimitsParams {
-                supports_luna_reserve: true,
+                supports_luna_reserve: false,
                 exclude_reset_credit_details: origin == RateLimitRefreshOrigin::Periodic,
             }),
         })
         .await;
     // Older remote app servers accept only null params. Keep their usage reads working
     // without opting them into exposure or pretending that they support the new capability.
-    if matches!(
+    let result = if matches!(
         &result,
         Err(codex_app_server_client::TypedRequestError::Server { source, .. })
             if matches!(source.code, -32600 | -32602)
     ) {
-        return request_handle
+        request_handle
             .request_typed(ClientRequest::GetAccountRateLimits {
                 request_id,
                 params: None,
             })
             .await
-            .wrap_err("account/rateLimits/read failed in TUI");
+    } else {
+        result
+    };
+    let mut response: GetAccountRateLimitsResponse =
+        result.wrap_err("account/rateLimits/read failed in TUI")?;
+    // Reserve is disabled in this build. Ignore unsolicited offers before they can
+    // change task settings or hold queued input waiting for a Reserve transition.
+    if response.rate_limit_upsell.as_ref().is_some_and(|banner| {
+        banner["banner_type"].as_str() == Some(crate::backend_banners::LUNA_RESERVE_BANNER)
+    }) {
+        response.rate_limit_upsell = None;
     }
-    result.wrap_err("account/rateLimits/read failed in TUI")
+    if let Some(fallbacks) = response
+        .rate_limit_upsell
+        .as_mut()
+        .and_then(|banner| banner.get_mut("fallback_model_slugs"))
+        .and_then(serde_json::Value::as_array_mut)
+    {
+        fallbacks.retain(|model| model.as_str() != Some(crate::model_catalog::LUNA_RESERVE_MODEL));
+    }
+    Ok(response)
 }
 
 pub(super) async fn fetch_thread_usage(
